# 2026-04-23 工作日志

## 项目概览探索
- 完整梳理了 devops-platform 的技术栈、4个Django App、数据库表结构、URL路由、部署架构

## 投产征集页面：添加序号列（行号网格）
- 文件：`myproject/templates/branch_create/release_entry.html`
- 在「当前批次记录」表格最左侧新增固定序号列（`#`），Excel 行号风格
- 实现方式：定义 `rowNoCol` 常量，在 `getActiveColumns()` 最前面插入，`renderItems()` 用 `index+1` 渲染
- 序号列固定在左侧（sticky left），宽44px，灰底风格，不出现在「自定义列」配置面板

## 投产征集页面：Excel 风格网格改造
- 文件：`myproject/templates/branch_create/release_entry.html`
- 表格外边框加粗：2px solid #475569（深灰），顶边+左边
- 单元格边框：1px solid #94a3b8（中灰），每个单元格四角都有边框
- 移除 th:first-child border-left:0 和 td:last-child border-right:0，使外边框完整不断裂
- 表头背景加深：#e2e8f0，底部 2px 边框，字重 700
- 序号列/操作列样式同步：去掉阴影效果，改用标准边框
